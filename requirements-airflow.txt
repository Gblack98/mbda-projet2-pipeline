# Environnement de l'ordonnanceur, separe de requirements.txt.
#
# Airflow et dbt ne peuvent pas cohabiter : ils exigent des versions
# incompatibles de google-cloud-*. Chacun son venv. Le DAG appelle dbt par le
# chemin explicite de son propre venv, jamais par le PATH.
#
# Version verifiee du DAG : Airflow 2.9.3 sous Python 3.12.
# Airflow 3.x l'importe aussi, mais les deux majeures different (BashOperator
# deplace dans le provider standard, cle `ts` retiree du contexte des
# callbacks). Voir docs/architecture.md, section Versions d'Airflow.
#
# Installation, le fichier de contraintes n'est pas optionnel :
#
#   python -m venv venv-airflow
#   ./venv-airflow/bin/pip install -r requirements-airflow.txt \
#     -c https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.12.txt

apache-airflow==2.9.3
google-cloud-bigquery==3.20.1
requests==2.32.3
