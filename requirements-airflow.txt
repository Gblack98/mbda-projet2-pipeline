# L'ordonnanceur seul. Airflow et dbt ne cohabitent pas, leurs versions de
# google-cloud-* sont incompatibles : chacun son venv.
#
# Teste avec Airflow 2.9.3 sous Python 3.12. Le DAG s'importe aussi sous 3.x,
# voir docs/architecture.md pour les ecarts entre les deux majeures.
#
# Le fichier de contraintes n'est pas optionnel :
#
#   python -m venv venv-airflow
#   ./venv-airflow/bin/pip install -r requirements-airflow.txt \
#     -c https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.12.txt

apache-airflow==2.9.3
google-cloud-bigquery==3.20.1
requests==2.32.3
