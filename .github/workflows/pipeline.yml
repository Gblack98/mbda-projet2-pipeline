name: Pipeline quotidien

# Seul declencheur planifie du projet : il tourne sans machine allumee. Le DAG
# Airflow couvre le meme perimetre mais reste en declenchement manuel, sinon
# deux WRITE_TRUNCATE concurrents ecriraient les memes tables.
#
# 18h37 et non 18h00 : GitHub met les workflows planifies en file d'attente
# quand la charge est forte, et la charge est maximale en debut d'heure. Une
# execution du 2026-08-07 lancee a 18h59 n'a demarre que le lendemain a 11h09.
# Une minute decalee reduit l'attente sans rien changer au calendrier.
on:
  schedule:
    - cron: "37 18 * * 1-5"
  workflow_dispatch:

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Cle de service
        env:
          GCP_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          mkdir -p ~/.gcp
          printf '%s' "$GCP_KEY" > ~/.gcp/mbda-projet2-sa.json

      - name: Profil dbt
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<'EOF'
          dbt_pipeline:
            target: dev
            outputs:
              dev:
                type: bigquery
                method: service-account
                project: crucial-bonsai-418120
                dataset: marts
                keyfile: /home/runner/.gcp/mbda-projet2-sa.json
                threads: 4
                location: EU
          EOF

      - name: Ingestion
        run: python scripts/ingest.py

      - name: Modeles et tests dbt
        working-directory: dbt_pipeline
        run: |
          dbt deps
          dbt build

      - name: Alerte par mail
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USER }}
          password: ${{ secrets.MAIL_PASSWORD }}
          from: Pipeline MBDA
          to: ${{ secrets.MAIL_TO }}
          subject: "Echec du pipeline — ${{ github.run_id }}"
          body: |
            L'execution du ${{ github.run_started_at }} a echoue.

            Etape en echec et journaux :
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
