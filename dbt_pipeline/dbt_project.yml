name: 'dbt_pipeline'
version: '1.0.0'
profile: 'dbt_pipeline'

model-paths: ["models"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
